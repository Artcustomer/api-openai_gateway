<nav>
    <ol class="breadcrumb">
        {% for pathItemIndex,pathItem in paths %}
            <li class="breadcrumb-item">
                {% if pathItem.path is not null %}
                    <a href="{{ path(pathItem.path) }}">{{ pathItem.title|trans }}</a>
                {% else %}
                    {{ pathItem.title|trans }}
                {% endif %}
            </li>
        {% endfor %}
    </ol>
</nav>
